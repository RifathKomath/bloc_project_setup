// import 'package:bloc/bloc.dart';
// import 'package:meta/meta.dart';

// import '../../../../core/network/notification_services.dart';

// part 'base_screen_event.dart';
// part 'base_screen_state.dart';

// class BaseScreenBloc extends Bloc<BaseScreenEvent, BaseScreenState> {
  
//   BaseScreenBloc({int initialIndex = 0}) : super(BaseScreenInitial(currentTabIndex: initialIndex)) {
//     on<BaseScreenEvent>((event, emit) {
//       // TODO: implement event handler
//     });

//      on<TabChangeEvent>((event, emit) {
//       emit(BaseScreenInitial(currentTabIndex: event.tabIndex));
//     });
//   }
// }
